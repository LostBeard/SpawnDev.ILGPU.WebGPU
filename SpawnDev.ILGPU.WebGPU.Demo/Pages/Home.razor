@page "/"
@using SpawnDev.ILGPU.WebGPU

<PageTitle>SpawnDev.ILGPU.WebGPU Demo</PageTitle>

<div class="hero-section">
    <div class="hero-content">
        <h1 class="hero-title">
            <span class="gradient-text">ILGPU + WebGPU</span>
        </h1>
        <p class="hero-subtitle">GPU-accelerated compute in the browser</p>
        <p class="hero-description">
            Run ILGPU kernels directly in your browser using WebGPU. 
            Write GPU compute shaders in C# and compile them to WGSL with support for high-precision 64-bit emulation.
        </p>
        <div class="hero-buttons">
            <a href="tests" class="btn btn-primary btn-lg">
                <span class="btn-icon">▶</span> Run Tests
            </a>
            <a href="mandelbrot" class="btn btn-secondary btn-lg">
                <span class="btn-icon">🎨</span> Mandelbrot Demo
            </a>
        </div>
    </div>
</div>

<div class="features-section">
    <h2 class="section-title">Features</h2>
    <div class="features-grid">
        <div class="feature-card">
            <div class="feature-icon">⚡</div>
            <h3>ILGPU Compatible</h3>
            <p>Use familiar ILGPU APIs - ArrayView, Index1D/2D/3D, math intrinsics, and more.</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">🔄</div>
            <h3>WGSL Transpilation</h3>
            <p>C# kernels are automatically compiled to WebGPU Shading Language (WGSL).</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">🌐</div>
            <h3>Blazor WebAssembly</h3>
            <p>Seamless integration with Blazor WASM via SpawnDev.BlazorJS.</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">🧩</div>
            <h3>64-bit Precision</h3>
            <p>Full support for double (f64) and long (i64) types via high-performance WGSL software emulation.</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">🧵</div>
            <h3>Shared Memory & Atomics</h3>
            <p>Supports workgroup shared memory, barriers, and atomic operations.</p>
        </div>
    </div>
</div>

<div class="code-section">
    <h2 class="section-title">Quick Start</h2>
    
    <div class="steps-container">
        <div class="step">
            <div class="step-number">1</div>
            <div class="step-content">
                <h3>Write Your Kernel</h3>
                <p>Define your GPU computation in standard C#.</p>
                <div class="code-block">
                    <pre><code><span class="keyword">static void</span> <span class="method">MyKernel</span>(<span class="type">Index1D</span> index, <span class="type">ArrayView</span>&lt;<span class="keyword">float</span>&gt; data)
{
    data[index] *= <span class="number">2.0f</span>;
}</code></pre>
                </div>
            </div>
        </div>

        <div class="step">
            <div class="step-number">2</div>
            <div class="step-content">
                <h3>Initialize Context</h3>
                <p>Enable the WebGPU backend in your ILGPU context.</p>
                <div class="code-block">
                    <pre><code><span class="keyword">var</span> builder = Context.<span class="method">Create</span>();
<span class="keyword">await</span> builder.<span class="method">WebGPUAsync</span>();
<span class="keyword">using var</span> context = builder.<span class="method">ToContext</span>();</code></pre>
                </div>
            </div>
        </div>

        <div class="step">
            <div class="step-number">3</div>
            <div class="step-content">
                <h3>Create Accelerator</h3>
                <p>Initialize the GPU device and accelerator.</p>
                <div class="code-block">
                    <pre><code><span class="keyword">var</span> device = context.<span class="method">GetWebGPUDevices</span>()[<span class="number">0</span>];
<span class="keyword">using var</span> accelerator = <span class="keyword">await</span> device.<span class="method">CreateAcceleratorAsync</span>(context);</code></pre>
                </div>
            </div>
        </div>

        <div class="step">
            <div class="step-number">4</div>
            <div class="step-content">
                <h3>Execute Kernel</h3>
                <p>Load and run your kernel on the GPU.</p>
                <div class="code-block">
                    <pre><code><span class="keyword">var</span> kernel = accelerator.<span class="method">LoadAutoGroupedStreamKernel</span>&lt;<span class="type">Index1D</span>, <span class="type">ArrayView</span>&lt;<span class="keyword">float</span>&gt;&gt;(MyKernel);
<span class="method">kernel</span>((<span class="type">Index1D</span>)length, buffer.View);
<span class="keyword">await</span> accelerator.<span class="method">SynchronizeAsync</span>();</code></pre>
                </div>
            </div>
        </div>

        <div class="step">
            <div class="step-number">5</div>
            <div class="step-content">
                <h3>Retrieve Data</h3>
                <p>Transfer the computed results back to host memory.</p>
                <div class="code-block">
                    <pre><code><span class="keyword">float</span>[] results = <span class="keyword">await</span> buffer.<span class="method">CopyToHostAsync</span>();</code></pre>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .hero-section {
        text-align: center;
        padding: 3rem 1rem;
        margin-bottom: 2rem;
    }

    .hero-title {
        font-size: 3rem;
        font-weight: 800;
        margin-bottom: 0.5rem;
    }

    .gradient-text {
        background: linear-gradient(135deg, #00d4ff 0%, #7c3aed 50%, #f472b6 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .hero-subtitle {
        font-size: 1.5rem;
        color: #94a3b8;
        margin-bottom: 1rem;
    }

    .hero-description {
        max-width: 600px;
        margin: 0 auto 2rem auto;
        color: #64748b;
        line-height: 1.6;
    }

    .hero-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }

    .btn {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.75rem 1.5rem;
        border-radius: 8px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.2s ease;
    }

    .btn-primary {
        background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
        color: white;
        border: none;
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(59, 130, 246, 0.4);
    }

    .btn-secondary {
        background: rgba(255, 255, 255, 0.1);
        color: #e2e8f0;
        border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .btn-secondary:hover {
        background: rgba(255, 255, 255, 0.15);
        transform: translateY(-2px);
    }

    .btn-icon {
        font-size: 1.1rem;
    }

    .section-title {
        font-size: 1.75rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        text-align: center;
        color: #f1f5f9;
    }

    .features-section {
        padding: 2rem 0;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.5rem;
    }

    .feature-card {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 12px;
        padding: 1.5rem;
        transition: all 0.2s ease;
    }

    .feature-card:hover {
        background: rgba(255, 255, 255, 0.08);
        transform: translateY(-4px);
        border-color: rgba(139, 92, 246, 0.3);
    }

    .feature-icon {
        font-size: 2rem;
        margin-bottom: 1rem;
    }

    .feature-card h3 {
        font-size: 1.1rem;
        font-weight: 600;
        color: #f1f5f9;
        margin-bottom: 0.5rem;
    }

    .feature-card p {
        color: #94a3b8;
        font-size: 0.9rem;
        line-height: 1.5;
        margin: 0;
    }

    .code-section {
        padding: 4rem 0;
        max-width: 900px;
        margin: 0 auto;
    }

    .steps-container {
        display: flex;
        flex-direction: column;
        gap: 3rem;
    }

    .step {
        display: flex;
        gap: 2rem;
        position: relative;
    }

    .step:not(:last-child)::after {
        content: '';
        position: absolute;
        left: 20px;
        top: 50px;
        bottom: -20px;
        width: 2px;
        background: linear-gradient(to bottom, rgba(139, 92, 246, 0.5), transparent);
    }

    .step-number {
        flex: 0 0 40px;
        height: 40px;
        background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
        color: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 1.25rem;
        box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
        z-index: 1;
    }

    .step-content {
        flex: 1;
    }

    .step-content h3 {
        font-size: 1.25rem;
        font-weight: 600;
        color: #f1f5f9;
        margin-bottom: 0.5rem;
    }

    .step-content p {
        color: #94a3b8;
        margin-bottom: 1rem;
        font-size: 1rem;
    }

    .code-block {
        background: #0f172a;
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 12px;
        padding: 1.25rem;
        overflow-x: auto;
        font-family: 'JetBrains Mono', 'Fira Code', 'Consolas', monospace;
        font-size: 0.9rem;
        line-height: 1.6;
        box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.3);
    }

    .code-block pre {
        margin: 0;
    }

    /* Syntax Highlighting */
    .keyword { color: #c678dd; }
    .method { color: #61afef; }
    .type { color: #e5c07b; }
    .number { color: #d19a66; }
    .comment { color: #5c6370; font-style: italic; }

    @@media (max-width: 640px) {
        .step {
            flex-direction: column;
            gap: 1rem;
        }
        .step:not(:last-child)::after {
            display: none;
        }
    }
</style>
